version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DATA_DIR=/data
      - SYSTEM_PASSWORD=${SYSTEM_PASSWORD}
    volumes:
      - ./data:/data
    restart: always
